# import "../fragments/userFragment.graphql"

mutation ($password: String!, $code: String!, $passwordConfirmation: String!) {
  resetPassword(password: $password, code: $code, passwordConfirmation: $passwordConfirmation) {
    jwt
    ...userInfo
  }
}
